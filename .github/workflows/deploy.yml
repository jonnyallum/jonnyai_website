name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "./Clients/jonnyai.website/package-lock.json"

      - name: Install dependencies
        run: npm install
        working-directory: ./Clients/jonnyai.website

      - name: Build project
        run: npm run build
        working-directory: ./Clients/jonnyai.website

      - name: Deploy to Hostinger via SFTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 92.112.189.250
          username: u384342620
          password: ${{ secrets.SFTP_PASSWORD }}
          port: 65002
          local-dir: ./Clients/jonnyai.website/out/
          server-dir: public_html/
          protocol: sftp
